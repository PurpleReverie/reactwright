// IEEE technical-report helper package. Bundles a Template that
// applies IEEE authority/branding but reflows it for longer-form
// single-column documents (white papers, working papers, internal
// IEEE reports). Pair with the userland `<Bibliography>` helper for
// numeric back-matter references.
export { Template, IEEE_REPORT_STYLES, IEEE_REPORT_CSS } from "./template.js";
