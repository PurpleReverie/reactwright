# @reactwright/cite-bibtex

BibTeX (`.bib`) adapter for Reactwright. Reads a `.bib` file at build
time and exposes its entries as a typed `createBibliography(...)`
map consumable by the engine's `<cite>` / `<bibliography>` primitives.

**Status: stub.** Coming soon. Tracking issue: TBD.
