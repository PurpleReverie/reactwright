# @reactwright/charts

SVG chart primitives (`<bar-chart>`, `<line-chart>`, `<scatter>`) for
Reactwright documents. Renders to inline SVG at content-tree
reconciliation time so the output paginates and prints cleanly.

**Status: stub.** Coming soon. Tracking issue: TBD.
