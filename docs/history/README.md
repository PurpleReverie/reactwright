# docs/history

Archived planning documents for changes that have already shipped.
Kept under version control for traceability (decisions made, options
considered, scope cuts) but moved out of the active `docs/` directory
so newcomers don't mistake them for current design intent.

For current state, see `docs/spec.md` (canonical) and
`docs/styling-spec.md` (styling dialect).
